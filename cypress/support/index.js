import "./commands";

// Parse creates the schema which causes the First insert to fail.
// Before all of the tests ensure we are starting with a clean DB.
before(function () {

});

// Placing beforeEach in the suuport directory, ensures
// that beforeEach runs before every 'it' block of the test
beforeEach(function () {

});

// Placing afterEach in the suuport directory, ensures
// that afterEach runs after every 'it' block of the test
afterEach(function () {

});
